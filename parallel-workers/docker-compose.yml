worker:
    #container_name: service_worker
    build: .
    environment:
        - MQ_HOST=13.235.16.166
        - MQ_USERNAME=worker
        - MQ_PASSWORD=worker
        - MQ_WORKER_QUEUE_NAME=worker_queue
        - MQ_ANALYZER_QUEUE_NAME=analyzer_queue
        - VIDEO_PATH=/tmp
    volumes:
        - /tmp/videos:/tmp
        
        